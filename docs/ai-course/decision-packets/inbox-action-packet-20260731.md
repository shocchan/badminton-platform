# Inbox Action Packet（2026-07-31・未返信問い合わせ5件＋不具合報告1件の実査結果）

技術Release Gateとは**別件**の運用タスク。個人情報（氏名・メール）はこのpacketに載せない。
実査方法: `contacts`（status='new'）と `ai_issue_reports`（未resolved）をread-onlyで全件確認。

## 結論サマリ

**「未返信5件」の実態は、返信が必要なのは1件だけ。** 4件は開発時の動作テスト（本文に「削除して構いません」と自記）。
不具合報告1件は実バグで、**本日修正済み**（下記C）。

## A. 問い合わせ5件の内訳

| # | 日付 | 区分 | 実態 | 重要度 | 推奨アクション |
|---|---|---|---|---|---|
| 1 | 7/6 | sponsor | フォーム動作テスト（「削除して構いません」と自記） | なし | closedへ変更 |
| 2 | 7/6 | activity | 同上（「フォームUI経由の動作確認です。削除OK」） | なし | closedへ変更 |
| 3 | 7/6 | other | 同上（「本番と同じREST経路のテスト。削除OK」） | なし | closedへ変更 |
| 4 | 7/6 | sponsor | 本文「あ」のみ（テスト起因とみられる） | なし | closedへ変更 |
| 5 | **7/22** | **tournament** | **実ユーザー**「申し込み完了できましたか？」（バドミントン側） | **中** | **返信＋entries確認** |

### #5 の返信ドラフト（バドミントン側・9日経過）

> お問い合わせありがとうございます。返信が遅くなり申し訳ありません。
> お申し込みの受付状況を確認いたしました（→ **entriesテーブルで該当申込の有無を確認してから**、
> 「受付済みです。当日お待ちしております」または「受付が確認できませんでした。お手数ですが再度お申し込みください」を選択）。
> ご不明点があればこのメールにご返信ください。

※ 送信はしていません（外部送信はCEOのみ）。申込者の照合はmembers/entriesをCEOが確認してください。

### テスト4件をclosedにするコマンド（CEOがOKなら私が実行します）

```bash
node scripts/ai-course/remote-sql.mjs --write --label "close form-test contacts" \
  --sql "update public.contacts set status='closed' where id in ('c1880cff-bf42-471c-b28f-951d519fabc0','f2258e54-293c-47b9-8113-e926ed3256b3','93c75b43-100c-4222-be3b-875b168e82fe','5e7862b5-ff77-449d-9a17-b2797f3ad214')"
```

（IDは実査で取得済み・4件とも本文がテスト自記のものだけを列挙。7/22の実ユーザー分は含めない）

## B. 不具合報告1件（7/25・/ja/ai-course・macOS）

- 内容: **「中国語にならない」**
- 実査結果: **実バグ（P1）**。`/zh/ai-course`（中国語LP）は完全に存在し表示できるのに、
  `/ja/ai-course` から切り替える導線が**ヘッダー・フッター・本文のどこにも0本**だった。
  中国語話者がjaのLPに着地すると中国語版に辿り着けない＝ターゲット顧客がまさに報告した症状。

## C. 対応（実施済み）

- LPヘッダーに「中文」⇄「日本語」切替リンク、フッターに「中文版」⇄「日本語版」を追加
  （`<a href>`＝JS無しでも遷移・SEOクローラも辿れる・aria-labelは切替先言語）
- 回帰テスト3件追加（`aiCourseLandingLangSwitch.test.tsx`）
- staging反映＋実画面確認（本packet末尾に結果を記入）

対応後、報告レコードを `resolved=true` にするのはCEO確認後（学習者向け回答が必要か判断のため）。

## D. 監視への反映

`daily-ops-check.mjs` が以後この2種（未返信・未対応報告）を毎日自動検知する
（今回の5件のうちテスト4件をclosedにすれば、アラートは実件数だけになる）。
