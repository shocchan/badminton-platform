> **【更新 2026-07-28】このパケットの判断は完了しました。**
> 教材P0/P1の14件はCEO判断済み・反映済み（root P0/P1 = 0）。
> **最新の状況と残る判断は `decision-packets/preproduction-blocker-summary.md` を見てください。**
> 以下は当時の記録として残しています。

# CEO判断パケット（自律ループ停止・2026-07-28 04:10）

監督ChatGPTが **decision = STOP_FOR_HUMAN** を出しました。
理由は「残る課題はすべて**人間の判断**か、AIに禁止されている変更を必要とするから」です。
**AI側の自律実装はここで止めます。** 以下はCEOに決めていただきたいことだけをまとめたものです。

---

## いま何ができている状態か

初回の学習体験（labPreview／staging限定）は、正常系だけでなく**異常系まで閉じています**。

- 初回4ステップの完走
- 診断・練習の途中で閉じても続きから再開
- 練習結果から復習予定が作られる
- 練習は終わったのに結果画面へ行けていない状態からの復帰
- アプリ更新でデータの版が食い違ったときの安全な案内（4パターン実機確認済み）
- 二重クリック・戻る操作でも二重記録されない
- 検証しても通常の学習記録が壊れない
- 200%拡大〜タブレット幅まで、横スクロールなし・タップ領域44px以上
- テスト790件・本番反映なし・共有DB未変更

---

## 決めていただきたいこと（7件）

### 1. 教材の判断（root P0=1件・root P1=13件）★最優先

`?app=1&vocab=1&vview=decisions` の判断キューで確認できます。

- **root P0（1件）**: fi-namae（名前）の例文
- **root P1（13件）**: cognate（日中で意味がずれる語）の判定ほか

各件について「採用する／今のままにする／こう直す／保留」のどれかと、その理由をください。
**AIが多数決や「ChatGPTとClaudeが一致したから」で代わりに決めることはしません。**

### 2. 教材を公開してよいか（human_reviewed / approved）

上の判断が済んだ語について、「レビュー済み」「公開してよい」を付けてよいかどうか。

### 3. 語彙の進捗と復習予定を正式にDB保存するか ★公開ブロッカー

**今は端末の中だけの試作保存です。** ブラウザのデータを消すと学習履歴が消えます。
別の端末では引き継がれません。この状態で受講生に出すと
「保存されていると思っていたのに消えた」が起きます。

決めていただきたいこと: 正式保存を実装するか／その場合のmigration適用の承認。
（共有Supabaseへの変更になるため、CEOの明示的な承認なしにAIは触りません）

### 4. admin_overrides の RLS ★公開ブロッカー

権限まわりの未解決問題です。RLS変更もCEO承認が必要な領域です。

### 5. 共通ボタンのfocus ring（全アプリ影響）

キーボード操作時の枠線の色が、共通ボタンでは 2.98:1 と基準（3:1）にわずかに届きません。
初回学習の画面だけは直しましたが、**全アプリ共通の部品を変えるかどうか**は判断が要ります。

### 6. 「ヒントを使った」を記録するか

今は記録していないため、まとめ画面に出せません（0と書くと「使わなかった」という
別の意味になるので出していません）。表示したい場合は記録の仕組みを足す必要があり、
学習ロジックに触れます。

### 7. 実機スマートフォンでの確認

タッチ操作・ソフトキーボード・読み上げ（VoiceOver / TalkBack）は
CEOの実機でしか確認できません。

---

## 確認していただきたい画面

staging: https://staging.badminton-platform.pages.dev
更新直後は `?cb=<数字>` を付けて開くと新しい画面が確実に出ます。

| 見るもの | 開き方 |
|---|---|
| 初回学習を通しで体験 | `?app=1&vocab=1` → 最下部「初回学習を安全に試す」 |
| 判断キュー（上記1の判断） | `?app=1&vocab=1&vview=decisions` |
| 学習の接続監査 | `?app=1&vocab=1&vview=connectivity` |

「初回学習を安全に試す」は検証モードです。**通常の学習記録には一切影響しません。**
終わったら「検証を終えて通常の画面へ戻る」で戻れます。

---

## AI側が勝手にやらないこと（念のため）

- 教材の採否をAIが決めること
- 共有Supabase・migration・RLS・認証・決済・本番反映・mainマージ
- 「たぶんこうだろう」で数値や訳を作ること

判断をいただければ、その範囲だけを実装して再びstagingでご確認いただきます。

---

## 参考資料

- 今夜の総括: `docs/ai-course/completion-reports/overnight-session-2-report.md`
- 各Phaseの詳細: `docs/ai-course/completion-reports/phase-2e1-13〜16-completion-report.md`
- 監督の指示文: `docs/ai-course/autonomous-loop/prompts/`
