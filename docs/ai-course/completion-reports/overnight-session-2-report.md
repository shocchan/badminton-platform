# 夜間セッション報告（2026-07-28 00:50 〜 04:00）

CEO指示:「続けて！ただ作るだけではなくて、見えやすくわかりやすく、視線移動しやすく、
なおかつ、イラストやアニメーション等でイメージしやすいつくりにして。朝8時半までやり続けていいよ。」

| 項目 | 値 |
|---|---|
| 実行Phase | **2E-1.13 / 1.14 / 1.15 / 1.16**（自律ループ #4〜#6） |
| コミット | 31件 |
| テスト | 688 → **790件**（+102） |
| tsc | 0エラー |
| lint | 45E/6W=51（ベースライン一致・**増分0**） |
| main bundle | 590.30KB（**増加0**） |
| 本番反映 | **なし**（stagingのみ・mainマージなし） |

---

## CEOのご指示への回答: 見た目をどう変えたか

インラインSVGだけで作りました（外部ライブラリも画像リクエストも増やしていません）。

| 追加したもの | ねらい | 場所 |
|---|---|---|
| ステップの絵4種（方位磁針／カードと虫めがね／吹き出し／カレンダーとチェック） | 各ステップが何をする所か絵で分かる | 初回Journeyの見出し横 |
| ステッパー（番号・✓・接続線） | 今どこか・あといくつか | Journey上部 |
| 「見る › ためす › ふりかえる」 | 「何問目か」だけでは見えない、語の中の今の一歩 | 練習画面 |
| 結果の横棒グラフ | 今日の結果を見て分かる | まとめ画面 |
| 復習タイムライン（今日→明日→3日後→7日後） | 「また戻ってくる」を時間軸で示す | まとめ画面 |

守った原則:
- **図は理解の補助。件数やラベルは必ず文字でも読める**
- 0件の項目は棒を描かない（0を強調して落ち込ませない）
- 色だけに頼らない（済み=✓・現在=塗り＋リング・未来=薄い丸と形も変える）
- 動きは `prefers-reduced-motion` 準拠で200〜500msだけ
- 絵は既定で装飾扱い（隣の見出しと二重に読み上げさせない）

---

## 実機で見つけて直した、学習者に実害のある不具合

すべて **staging実機で操作したから見つかった**もので、ユニットテストでは出ませんでした。

1. **今日のことばの結果が復習予定に入っていなかった**
   まとめ画面が「忘れかけるころにもう一度出てきます」と言うのに予定が0件だった。
   学習ループが閉じていない状態。接続して回帰テストを追加。

2. **診断を12問答えて再読込すると1問目からやり直し**（しかも別の問題）
   出題セットが学習記録から作られるため、作り直すと変わってしまう。
   確定時点のセットごと保存して同じ問題の続きから再開するようにした。

3. **練習の2語目で再読込すると1語目のカードからやり直し**
   確定した操作だけを再開位置として保存するようにした
   （選んだだけで確定していない回答は保存しない＝再読込で正解扱いにしない）。

4. **まとめ画面の内訳が全部0**（今日確認3・自分でできた0・ヒント0・もう一度確認0）
   クイズ正誤と自己評価という別の軸を、ひとつの合計の内訳のように並べていた。
   3つの軸に分けて表示するよう作り直した。

5. **200%拡大でタップ領域が31pxしかない／横スクロールが出る**
   タブを折り返すようにして、188px幅でも44pxを確保しつつ横あふれ0にした。

6. **修復は保存されるのに画面が1つ前のまま**（Recovery動作時）
7. **「続き方が読めません」画面の第一CTAが「ホームへ」だった**（本来は「最初から始める」）

---

## 学習者にとって何ができるようになったか

- 学習の**途中でページを閉じても続きから戻れる**（診断も練習も）
- 練習は終わったのに結果画面へ進めていないとき、**学習をやり直さずに結果だけ見に行ける**
- アプリ更新で保存データが食い違っても、**勝手に完了扱いにならず**安全に案内される
- まとめ画面で「問題の結果」「自分の感じ方」「次の復習」が**別々に**分かる

## 検証のやり方が安全になった

ことば図鑑の最下部に「**初回学習を安全に試す**」を追加しました。
検証モード中は通常の記録を読みも書きもせず、終了時は検証用のデータだけ消えます。
今回の検証はすべてこの中で行い、CEO端末の学習記録（8,960文字）と復習予定は**一度も変化していません**。

---

## CEOに見ていただきたい画面

staging: https://staging.badminton-platform.pages.dev
更新直後は `?cb=<数字>` を付けて開くと新しい画面が確実に出ます。

1. `?app=1&vocab=1` → ことば図鑑トップ
2. 最下部の「初回学習を安全に試す」→ 検証モードで初回Journeyを最初から体験できます
3. 目的を選ぶ → 短い確認 → 3語の練習 → まとめ（結果の3カードと復習タイムライン）
4. 「検証を終えて通常の画面へ戻る」で元に戻ります

---

## CEOの判断が必要なこと

| 項目 | 内容 |
|---|---|
| **root P0 = 1件** | fi-namae の例文 |
| **root P1 = 13件** | cognate（日中で意味がずれる語）の判定 |
| **正式DB保存** | 語彙の進捗と復習予定は今も端末内のpreview保存のみ。正式公開のブロッカー |
| **admin_overrides の RLS** | 正式公開のブロッカー |
| ヒント使用の記録 | 「ヒントを使った」を表示したい場合、記録の仕組みを足す必要があります（学習ロジックに触れるため判断が要ります） |
| 共通ボタンのfocus ring | 全画面共通のため今回は範囲外。全アプリに影響する変更なので判断が要ります |
| 実機スマートフォン確認 | 実際の端末での操作感 |

---

## 次の再開地点

- 監督チャット「AI日本語学習監督」に 2E-1.16 の完了報告を送信済み。次Phaseの指示待ち
- 依頼文は `docs/ai-course/autonomous-loop/prompts/` に保存
- 各Phaseの詳細は `docs/ai-course/completion-reports/phase-2e1-13〜16-completion-report.md`
