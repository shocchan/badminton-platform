# kawabado 引き継ぎ書（2026-09-10）

kawabado（川口・蕨バドミントン交流会）と、そこから生まれたAI日本語学習システム「日本語の相棒」の現在地。
**新しいセッションと ChatGPT に、同じ前提で入ってもらうための一枚。**

数字はすべて本番DBの実測。推定は推定と書いてある。

- 本番ビルド: `index-CpFwtE-Q.js`
- Supabase: `jdkwijdphlkrcoiggfqw`
- Web版（同じ内容・読みやすい）: https://claude.ai/code/artifact/b67bb37f-e43c-43b4-9c50-ef379571947e

---

## 01. まず全体像

ひとつの会社が3つの事業を持っている。共通しているのは**川口・蕨に住む人**で、そこから2方向に伸びている。

| 事業 | 誰に | 売るもの | 状態 |
|---|---|---|---|
| **バドミントン交流会**（kawabado.com） | 地域の社会人 | 通常活動 600円〜／大会 1,000円〜 | 稼働中 |
| **AI日本語学習**（study.kawabado.com） | 在日の中国語話者 | 600円／2,980円／10万円 | 稼働中 |
| 日本人向け自動収益（note・ココナラ） | 副業志向の日本人 | 有料記事ほか | 別系統 |

この引き継ぎ書が扱うのは上の2つ。3つ目（軸2）は担当と場所が分かれていて、ここには含まない。

---

## 02. AI日本語学習システムの現在地

| 指標 | 値 |
|---|---|
| 実在の学習者 | 8人 |
| 有効な受講権 | 13件 |
| 自動決済の成立 | 3件 |
| 自動決済の売上 | 1,200円 |
| 音声会話の実績 | 37回 |
| 配布済み個別URL | 9本 |

**この数字の読み方**：売上1,200円は**セルフサービス決済だけ**の数字（600円の体験パス1件と、テストを兼ねた回数券300円×2件）。10万円の伴走コースは人が対応する手動契約なので、ここには入っていない。受講権13件と学習者8人がずれるのは、検証用・QA用のアカウントが5件混ざっているため。

### 学習者の内訳（実在の8人）

| 目的 | 申告 | 目標 | 出る教材 | 人数 |
|---|---|---|---|---|
| 会話 | N1 | — | N1 | 2 |
| 併用 | N1 | N1 | N1 | 1 |
| 試験・併用 | — | N2 | N2 | 2 |
| 試験 | — | N3 | N3（単語はN5から） | 1 |
| 試験 | — | N5 | N5 | 1 |
| 一度も開いていない（8/18開通・伴走コース） | | | | 1 |

**未開封が1人いる理由**：伴走コースの生徒1人は、8/18に開通してから学習者の行が作られていない（最初の名前入力を通過していない）。ただし**これはアカウントを渡した当時、ID・パスワードの入力が必要だった時期の話**。9/9からURLを押すだけで入れるようになったので、ここは配り直しで解ける見込み。（CEO判断：追加の対応は不要）

### 見込み客の入口

| 面 | 人数 |
|---|---|
| WeChat「日本語授業」 | 52 |
| WeChat「見込み」 | 194 |
| 元レッスン生 | **40人以上**（当初15人と見ていたが数え直したら40人超） |
| 小紅書フォロワー | 5,592（4/17時点・別系統の集計） |

配るのは `study.kawabado.com` のURL1本。

---

## 03. 商品と価格

価格の正準は `src/lib/aiLesson/course/plans/planCatalog.ts`。**ここ以外に金額を書かない**という決まりがあり、テストで固定されている。

| 商品 | 価格 | 期間 | 音声会話 | 決済 |
|---|---|---|---|---|
| 7日間の実力診断 | 無料 | 7日 | 週3回の共通枠 | 招待リンク |
| AI体験パス | 600円 | 開始から7日 | 合計3回 | カード直行 |
| 1か月 AI自学プラン | 2,980円 | 30日 | 合計8回 | カード直行 |
| 6か月 AI日本語伴走コース | 10万円 | 6か月 | 180回 | 相談（人が対応） |
| AI会話 1回（回数券） | 300円 | — | +1回・6分 | カード直行 |
| AI会話 5回券 | 1,350円 | — | +5回・6分 | カード直行 |

### AI会話の扱い（9/9に方針変更）

音声会話だけが原価を持つ（1分 約25円・モデル値）。教材・冒険・バトル・模試・復習は端末内で完結して原価ゼロ。そこで**AI会話をベータ扱いにして、毎日の導線から外した**。

- 毎日の「今日の冒険」には**出さない（全員）**
- 「ほかの学習を見る」から自分で選んだ人だけが使う
- **週3回まで・全員**（直近7日のローリング。暦週だと連日で燃やせるため）
- 超えたら回数券。使い切っても「あと◯日で1回もどる」を必ず出す

**なぜ全員に枠をかけたか**：それまで音声の枠は**購入プランの人にしか無かった**。手動発行の生徒（`plan_id` が null）は共通上限の1日10回のままで、1人あたり1日 最大1,000円・月80回なら8,000円。無料枠100人の見積り30,000円もここが原因だった。**いちばん大きなコストの穴**だったので、プランの有無に関係なく週3回で頭打ちにした。

### 回数券の採算

| 商品 | 実収 | 原価 | 原価率 |
|---|---|---|---|
| 都度 300円（6分） | 289 | 150 | 52% |
| 5回券 1,350円 | 1,301 | 750 | 58% |

Stripe手数料3.6%を引いた実収。上限60%（体験パスに設けた基準）の内側。テストで固定してある。

> **この原価は実測ではない。** 音声はWebRTCでブラウザがOpenAIへ直接つながるので、こちらで観測できるのは**分数だけ**。1分25円はモデル値（$0.1344/分 × 安全率1.2 × 155円/USD）で、**OpenAIの実請求と一度も突き合わせていない**。300円という値決めがこの数字に乗っているので、`scripts/ai-course/reconcile-openai-cost.mjs` での突合は早めにやったほうがいい。

---

## 04. 入り方（ここが事業の急所だった）

離脱の最大原因は教材ではなく**入れないこと**だった。WeChatのチャットではURLもIDもパスワードも同じように埋もれる。そこで入口を作り直した。

| 状況 | 入り方 | 備考 |
|---|---|---|
| **ふだん** | 個別URL `/ja/learn/XXXX-XXXX-XXXX` | 押すだけ。ログイン不要 |
| 検索でサイトに来た | ID（＝申込メール）＋パスワード | ログイン画面の既定 |
| どちらもだめ | 先生 / `info@kawabado.com` | 唯一埋もれない経路 |

WeChatで渡すのは**URL1本だけ**。コードを併記していたのをやめた。3つ渡しても3つとも埋もれるだけで、復旧の役に立たないため。文面には必ず3点を入れる ―
**收藏に入れる／外に出さない／サイトから入るときはID＋パスワードが要る**。

> **認証がいま二重になっている。** 認証アカウント20件のうち**14件が合成メール**（`s7fc67a3@id.badminton-platform.pages.dev`）で、6件が実メール。合成メール勢は**パスワード再設定メールが物理的に送れず**、バドミントン側のマイページにも会員として出てこない。9/9から**新規購入者は実メール**で作るようにしたが、既存14人は移行していない。

---

## 05. 9/9〜9/10 にやったこと

2つのセッションが並行で入っていた。両方まとめてある。コミット43件、すべて本番反映済み。

### 入口と継続

- 個別URL（学習コード）の発行と、WeChat用の文面
- ログイン画面の既定を ID＋パスワードへ。IDはメールアドレスも受ける
- 購入者に個別URLを発行し、購入メールに載せる
- ログイン後に**利用期限**を表示（手動契約の生徒にも。9人が見えていなかった）
- 期限切れを行き止まりにせず、その場で3択＋問い合わせ
- ブラウザの戻るでアプリごと閉じるのを修正

### お金まわり

- 決済の観測（`ai_payment_events`）を追加。沈黙を証拠に変えた
- 受講権が付かない障害の修正（RPCが本番に無かった）
- 大会の決済をAIコースのwebhookが400で突き返していたのを停止
- 回数券（300円／1,350円）の販売・付与・消費
- 紹介制度。報酬は**相手が3日学習したら**出る（購入時点ではない）

### 学習の中身

- N1保持者にN2教材が出ていたのを修正。**持っている級と目指す級を区別**する
- 単語をレベルから選んで学ぶ（自分の級から下は全部）
- AI会話で**話す場面を自分で選ぶ**
- 「学習した日」の定義をサーバーとアプリで統一
- 3日離脱メール、言い直しの練習、消える復習の修正、ことわざの読み上げ修正
- 7日間の実力診断（無料）を招待リンクで配れるように

### 今日みつけて直した不具合

| 症状 | 原因 |
|---|---|
| 個別URLがどれも「先に入っていた人」の画面に着く | セッションがあればコードを使わない近道。共用端末で他人の記録が見える状態だった |
| 払ったのに回数券の残高が増えない | 関数に `grant execute to service_role` を書き忘れ |
| 名言図鑑が初回から「60/60」 | 足す→選び直される→また足す、が1回の表示で60周するループ |
| ラリー自己ベストが全アカウント同じ | localStorage（端末に1つ）を読んでいた |
| AI会話が「今日あと10回」 | 週の枠になったのに1日の共通上限を表示していた |
| N1の人にN2ミニ模試 | 級の判定に `targetJlpt` を直接使い、会話目標（null）が既定のN2に落ちていた |

---

## 06. 触る前に知るべきこと

### やってはいけない

- **kawabado.com への直接デプロイ**（必ず staging で確認してから）
- **本番での破壊的な検証**
- **生徒アカウントへのログイン**（記録が汚れ、AI原価を消費し、本人の操作と区別がつかなくなる）
  - 生徒の画面を見たいときは検証用コピーを作る手順がある

### 唯一の正しい手順

| やること | 手順 |
|---|---|
| 本番SQL | `scripts/ai-course/remote-sql.mjs`（既定は読み取り。`--write` は監査ログに残る） |
| Edge Function | `scripts/deploy-edge-functions.sh <fn>`（`verify_jwt` を反映後に検証する） |
| サイト（staging） | `scripts/deploy-staging.sh` |
| サイト（本番） | `scripts/deploy-production.sh`（`integration/unify-2026-08-28` からのみ） |
| 型検査 | `tsc -b`（`--noEmit -p tsconfig.json` は何も検査しない） |

### 過去に実際にやられた地雷

- **verify_jwt**：未ログインから呼ぶ関数をJWT検証ONで出すと全部401。監視が毎日401で落ちていたのに cron は「成功」と表示していた
- **StripeはAIコース専用ではない**：大会の決済も同じwebhookに届く。知らない決済は200で見送る（4xxで突き返すとStripeがエンドポイントを無効化しにいく）
- **ワークツリー共有**：複数セッションが同じフォルダで作業する。他人の未コミットを巻き込まないこと
- **保護リスト**：実在生徒のIDを含む書き込みSQLは既定で拒否される（`--allow-protected` は監査に残る）

---

## 07. 決まっていないこと

| 件 | 中身 |
|---|---|
| **1か月プランの音声回数** | 売り文句は「月10回」、実際に使えるのは8回。8/28に原価率を下げるため8にしたとき文言が取り残された。表示を8に直すか、実体を10に戻すか（戻すと原価率48.4%で自分で決めた上限45%を超える） |
| **合成メール14人の移行** | 移行すればパスワード再設定が使えるようになり、バド側のマイページにも出る。ただし全員に新しいログイン情報を配り直すことになる |
| **原価の実突合** | 1分25円はモデル値。300円という値決めがここに乗っている。OpenAIの実請求と突き合わせる |
| **紹介クーポンのID** | 招待された人への50%OFFは、Stripe側のクーポンIDが未設定のあいだ**効かない**（黙って通常価格で通る）。設定すれば有効になる |
| **学習コードの手入力欄** | ログイン画面の既定からは外したが、まだ消していない。合成メール勢が移行中で、URLもパスワードも手元に無い人の最後の道になっているため。全員が移行したら外す |

---

## 08. この先に効きそうなこと

1. **まず5人ほどに配って様子を見る。** 無料の7日間実力診断と個別URLは用意できている。40人・194人へ一度に配ると、無料枠のAI原価が読めないまま乗る。少人数で反応を見てから広げる
   ```bash
   node scripts/ai-course/issue-alumni-code.mjs --count 5
   # --confirm を付けるまで実際には発行しない
   ```
2. **元レッスン生は40人以上いた**（当初15人と見ていた）。文面は `docs/ai-course/marketing/FREE_TRIAL_COPY.md` に用意済み（試験対策を押さない書き方に直してある）
3. **分野のテーマ。** いまテーマは「級」で並んでいる。語彙データに分野タグ（買い物・仕事・病院…）が無いため。タグを付けるのはコードではなく教材の仕事
4. ~~**N1の聴解音源が無い。**~~ → 同日の基盤フェーズ（下記 09）で 38 本を作った。N1 の模試に聴解が入る（本番 deploy 待ち）

---

## 09. 同日の追加作業 — N5〜N1＋Business Japanese 学習基盤（Phase 1〜8・12 commits・本番未反映）

CEO 指示（2026-09-10）で、教材を増やすのではなく**学習者ごとに「何を知っている／解ける／言えない／忘れかけ／どの場面で使えない」を
知識項目単位で理解して次の学習へつなぐ**基盤を作った。最終報告は `docs/ai-course/knowledge/FINAL_REPORT_2026-09-10.md`。

| Phase | 何を | commit |
|---|---|---|
| 1 | 既存教材を知識グラフとして読む（ID は振り直さない） | 85c9ef35 |
| 2-1 | 語彙に Practical Axis（10分野×9場面。例文は根拠にしない） | 1e8927f9 |
| 2-2 | 未解決表現 265 件の分類・Alias 層・ギャップ教材 23（draft・未公開） | 6677b9b7 |
| 2-3 | **N1 聴解 0→38**（事前生成音声） | 37b62196 |
| 2-4 | **N1 語彙 317→513**（教材コーパス逆算） | fc243107 |
| 2-5 | **N1 読解 30→42**（短文：仕事の文書・評論の断片） | 0cf38ba8 |
| 3 | recognition／production／conversation の分離・Next Best Action（台帳は読むだけ）。**vocab-n1 が無く N1 語がバトルに出ていなかった不具合も修正** | c658b49d |
| 4 | 文法↔語彙↔読解↔聴解↔産出↔分野の接続（22,848 辺・根拠つき） | 65ddccc9 |
| 5 | Business Japanese を層として（既存文法・語彙の参照のみ） | 00755bec |
| 6 | AI会話の結果を grammarId へ。文法の practice を会話ミッションに（週3回・任意選択は維持） | a43e5cea |
| 7 | 割り込み UX（adaptive）。**本番は半二重のまま**、QA/staging と `?interrupt=adaptive` だけ | 0e6cd437 |
| 8 | Realtime テキスト出力単価 16→24 の migration を**準備のみ**（本番未適用） | 7d8c8285 |

**CEO 確認待ち**: 本番 deploy／migration の本番適用（価格影響なし）／ギャップ教材 23 件の公開／adaptive 割り込みの本番反映。
実在生徒データへの変更 0・本番 DB write 0・deploy 0。テスト 5,384 PASS。

### 09-2. 本番反映（同日・CEO 承認後）
- **本番 deploy 2 回**: 0b3835db（Phase 1〜8 全部）→ 81417d57（目標レベル選択の注記修正＋Alias 4 件）。門 (a)(b)(c)(d) 全通過。
- **QA アカウント（n5copy）で本番 smoke**: N5／N3／N1 の各目標でルート生成・今日の冒険・復習7問（錯題ノート30件）が回帰なし。
  N1: 総合ミニ模試が 言語知識10・読解5・**聴解5** の3科目に（以前は「一部科目」）、「N1のことば」に batch 49 の語（紛れる）が出る、
  AI会話の場面選択に「今日の文法」＋「仕事の場面」30件（N1 の級で全部）。最後に元の N2 目標へ戻した（1日の学習時間は 15分に。元は 3 step 10分）。
- **DB migration は未適用**: 本番に `public.ai_model_prices` が**存在しない**（`20260824150000_ai_usage_events` が本番へ当たっていない。
  `ai_usage_daily`／`ai_cost_topups` はある）。16→24 の update は対象テーブルが無いので実行されなかった。
  先に usage-events の migration（新テーブル作成）を当てるかは CEO 判断。TS 側は 24 に直してあり、テストは通っている。
- **adaptive 割り込みの実機 QA（Case A〜G）は未実施**: マイクを使う検証は自動ブラウザではできない。
  確認できたこと＝staging の配信物に方針コードが入っている／`?interrupt=adaptive` が `/ja/ai-course` に残る／本番は半二重のまま（テスト）。
  QA アカウントは AI会話の週3回を使い切っているため、人が試すには回数の付与（QA アカウント限定の DB write）が要る。
- 既知の無害な 500: `rpc/ai_upsert_unit_progress` は同期の probe（expected_row_version=-1 → P0409）で、書き込まない設計どおり。
- 次フェーズ候補（記録のみ）: N3〜N5 の基礎語（必要・理由・方法・意見・予定…）がバンクに無い（PHASE2_4）。ギャップ教材の判断は `GAP_DRAFTS_REVIEW.md`。

---

2026-09-10 時点。数字は本番DBの実測、原価はモデル値。
詳細は `docs/ai-course/` と各ファイル冒頭のコメントに、「なぜそうしたか」まで書いてある。
